{
  "name": "apothecary-shop",
  "version": "1.0.0",
  "description": "Pharmaceutical Inventory System",
  "main": "index.js",
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd ui && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && cd server && npm install && cd ../ui && npm install",
    "update-all": "cd server && npx npm-check-updates -u && npm install && cd ../ui && npx npm-check-updates -u && npm install",
    "build": "npm run install-all && cd ui && npm run build",
    "start": "node server/server.js"
  },
  "dependencies": {
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^7.3.1"
  }
}
